import { createTheme } from "../stitches.config";

export const darkTheme = createTheme({
  colors: {
    primary: "#56B1D5",
    secondary: "#1C2640",
    background: "#0F1626",
    text: "#F3F3F4",
    accent: "#448BCA",
  },
});
